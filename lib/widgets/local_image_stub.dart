import 'package:flutter/material.dart';

/// Web stub. There is no local file system on web, so a stored local photo
/// can never be shown; callers fall back to the drawn artwork instead.
Widget? localImage(
  String path, {
  required double size,
  required double borderRadius,
}) =>
    null;
