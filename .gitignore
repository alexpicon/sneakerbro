# Flutter / Dart build artifacts
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# OS
.DS_Store

# Platform runner folders. Regenerated by `flutter create --platforms=...`
# (see Running it in the README). The iOS Info.plist permissions need to
# be re-added after regeneration.
android/
ios/
web/

# Hive writes its boxes into the app's documents directory at runtime,
# so there is nothing to ignore here for storage.
